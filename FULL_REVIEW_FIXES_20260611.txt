Full review fix package - 2026-06-11

Fixed:
- Added favicon.ico to deployed public root to stop /favicon.ico 404.
- Added admin-menu-compat.js before admin-workflow-fix-v34.js to define getMenuItems/setMenuItems globally before legacy workflow patches run.
- Updated admin.html cache versions so Railway/Chrome load the patched scripts.
- Hardened admin login token persistence across legacy token keys used by older admin scripts.
- Patched admin auth guard to verify /api/admin/me with Authorization bearer token.
- Added CSP script-src-attr allowance because this legacy admin UI still uses inline onclick/oninput handlers.

After deploy:
1. Hard refresh admin page with Command+Shift+R.
2. If admin still shows old script versions, clear browser site data for the Railway domain.
3. Re-login as super admin.
